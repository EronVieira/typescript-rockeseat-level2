yarn init -y

yarn add typescript -D

yarn add express

yarn add -D @types/express

yarn tsc src/index.ts     -> Executa arquivo typescript

yarn tsc --init      -> Inicializa configuracoes do Typescript e cria o arquivo tsconfig.json

yarn tsc            -> Converte todos os arquivos Typescript para Javascript

Altera a flag outDir no tsconfig.json para => "outDir": "./build", 